package org.ubiquia.core.belief.state.generator.model.json.schema;

/** Classifies a JSON Schema definition by its JPA mapping role. */
public enum ModelType {

    EMBEDDABLE,
    ENTITY,
    ISOLATED,
    ENUM
}
