package {{package}};

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.ubiquia.common.library.belief.state.libraries.service.mapper.AbstractEgressDtoMapper;

@Service
public class {{classname}}EgressDtoMapper extends AbstractEgressDtoMapper<{{classname}}Entity, {{classname}}> {

    private static final Logger logger = LoggerFactory.getLogger({{classname}}EgressDtoMapper.class);

    @Override
    public Logger getLogger() {
        return logger;
    }

    @Override
    public {{classname}} getNewDto() {
        var dto = new {{classname}}();
        dto.setModelType("{{classname}}");
        return dto;
    }

    @Override
    public String getModelType() {
        return "{{classname}}";
    }
}
